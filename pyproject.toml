[project]
name = "ifconfig-py"
version = "0.4.0.post1"
description = "A Python implementation of ifconfig.me, built with FastAPI"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.128.0",
]
